apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-consumer
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-consumer
  template:
    metadata:
      labels:
        app: notification-consumer
    spec:
      containers:
      - name: notification-container
        image: sjangra314/notification-consumer:latest  # Replace with your Docker image
        ports:
        - containerPort: 80
        env:
        - name: KAFKA_BROKER
          value: "kafka:9092"  # Kafka service name and port
        resources:
          requests:
            memory: "256Mi"
            cpu: "500m"
          limits:
            memory: "512Mi"
            cpu: "1000m"